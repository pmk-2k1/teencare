# TODO - Step39 UI update

## Plan summary
- Replace `src/steps/phase4/step39.tsx` UI to match the provided image layout ("kiểu 2": update both layout and text per image).
- Keep `onNext` wiring and `t("continue")` button behavior.
- Use existing SVG icons (`CheckCircle`, `CloseCircle`, `Shield`) and Tailwind styles consistent with repo.

## Steps
1. Inspect current implementation of `src/steps/phase4/step39.tsx` (done).
2. Compare with the provided image to extract exact text + layout structure.
3. Edit `src/steps/phase4/step39.tsx` to match the image.
4. Run typecheck/build (`npm run build` or `npm run lint`) to ensure no TS/ESLint errors.

